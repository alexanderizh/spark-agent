# 三大模块升级改造 —— 测试用例汇总

> 状态: 待验证 | 最后核对: 2026-07-04
>
> 覆盖范围：**记忆系统 V2**（M1-M4 全链路）、**团队模式 A2A 深度协作 + 成员自由交流 v2**（Phase 0-G）、**工作流升级**（原子节点真实执行 + 条件分支 + 模板库）。
>
> 用例来源：对照近 14 天 commit（`f0e2b1b11` 起）实地核查源码与现行 `todo/` 设计文档，**只列已落地能力的测试点**。
>
> 用例分级：
> - 🟢 **自动（A 级）**：可在 CI 跑的 vitest 单测/集成用例，附建议落点文件。
> - 🔵 **集成（B 级）**：需真实 DB / 真实 provider 的集成脚本（`pnpm --filter @spark/agent-runtime test`）。
> - 🟠 **真机（C 级）**：`pnpm dev` 启动 desktop 手测，无法自动化。
>
> 每条标注「**已覆盖**」（现有测试已写）/「**建议补**」（缺口）。

---

## 模块一：记忆系统 V2

> 现状：单测 95+ 全绿，gate evals 16/16、search evals 11/11。本节重点是**审查修复点（H1-H7/M9-M16）的回归覆盖**与**真机全链路**。

### A1. 写入与抽取链路

| # | 用例 | 等级 | 落点 / 状态 |
|---|---|---|---|
| M-A1.1 | 抽取 prompt 注入「当前会话上下文」（workspaceId/agentId + 是否项目会话），LLM 能据此把"这个项目我独自开发"归 `project` 而非 `user` | 🟢 自动 | `memory-extraction.prompt.ts` 单测 **建议补**（断言 prompt 含 workspaceId 段 + scope 判定提示） |
| M-A1.2 | `parseCandidates` 对非法 scope/type 枚举值硬拒绝（M14），不污染下游 | 🟢 自动 | `memory-writer.service.test.ts` **建议补**（feed 非法 enum → 该候选 skipped） |
| M-A1.3 | `processCandidate` per-candidate try/catch（H7）：一条候选抛错时其余仍写入，日志含 `processCandidate failed (isolated, rest continue)` | 🟢 自动 | `memory-writer.service.test.ts` **已部分覆盖**，建议补"mock 第 2 条 insert 抛错，断言第 1/3 条仍落库" |
| M-A1.4 | `0 候选` / `LLM 返回脏数据`：日志提级 info/warn（可观测），主对话不被阻塞 | 🟢 自动 | `memory-writer.service.test.ts` **建议补** |
| M-A1.5 | extraction provider 不可用 → `complete()` 降级返回 `'[]'`，turn 正常结束 | 🟢 自动 | `memory-writer.service.test.ts` **已覆盖**（无 provider 路径） |
| M-A1.6 | ModelService.complete 三分支回退：OpenAI / anthropic `/v1/messages` / null | 🟢 自动 | `model.service.complete.test.ts` **已覆盖**（commit `a0f140ea6` 修类型后） |

### A2. 演化决策 + bi-temporal 失效链（M2，核心审查修复区）

| # | 用例 | 等级 | 落点 / 状态 |
|---|---|---|---|
| M-A2.1 | **H1/H6**：演化 DELETE/MERGE 后失效条目释放 name 槽，同名新条目不再撞 UNIQUE（索引含 `invalid_at IS NULL`） | 🟢 自动 | `memory-writer.evolution.test.ts` **建议补**：先写 `usr_a`（webpack）→ DELETE → 再写同名 `usr_a`（vite），断言不抛 UNIQUE |
| M-A2.2 | **H3/M5**：`listByScope`/`countByScope`/`findEvictionCandidates` 默认过滤 `invalid_at IS NULL`，失效条目不进 prompt/配额/淘汰 | 🟢 自动 | `memory.repository.test.ts`（storage） **建议补**：插入失效条目，断言三方法不返回 |
| M-A2.3 | DELETE：旧条目 `invalid_at` 非空 + `superseded_by` 指向新条目；FTS 搜旧词不再返回失效条目 | 🔵 集成 | 切 Node ABI 跑真实 DB **建议补** |
| M-A2.4 | UPDATE：原条目 `id` 不变 + `hit_count` 保留 + `description` 更新；文件正文末尾出现 `## History` | 🔵 集成 | `memory-e2e.test.ts` **已覆盖** |
| M-A2.5 | NOOP：重复纠正第二次判 NOOP，无新条目 | 🟢 自动 | `memory-evolution.service.test.ts` **已覆盖** |
| M-A2.6 | recall 失效 id：返回 ⚠️ 失效标注，不当作活跃记忆使用 | 🟢 自动 | `memory-reader.service.test.ts` **建议补** |

### A3. 检索（FTS5 + 向量 RRF + 实体一跳）

| # | 用例 | 等级 | 落点 / 状态 |
|---|---|---|---|
| M-A3.1 | **H5**：CJK 二字词命中（"迁移"），`segmentCjk` 逐字分词 + phrase 保序 | 🟢 自动 | `segment-cjk.test.ts`（storage） **已覆盖** |
| M-A3.2 | **H5**：多词英文共现（"react hooks"），CJK 段 phrase + 英文段 AND，不要求紧邻 | 🔵 集成 | `memory-reader.search.test.ts` **已覆盖** |
| M-A3.3 | **M9**：ADD insert 传 body，FTS 索引正文，"甲骨文"在 body 也能命中 | 🔵 集成 | `memory-reader.search.test.ts` **建议补** |
| M-A3.4 | **H2**：EmbeddingService/MemorySearchRepo 为 SessionService lazy 单例，embedding 宕机时 `unavailableUntil` 负缓存跨 turn 持久（不每轮阻塞 15s） | 🟢 自动 | `memory-search.service.test.ts` **建议补**：第一次失败设负缓存，第二次直接短路 |
| M-A3.5 | **S5.1 降级**：无 embedding → FTS-only，日志 `vector=disabled (FTS-only)`，search 仍返回结果 | 🟢 自动 | `memory-search.service.test.ts` **已覆盖** |
| M-A3.6 | 实体一跳扩展：共享 `arco` 实体的两条记忆，搜其中一条时返回另一条（"经实体关联的其他记忆"段） | 🔵 集成 | entity 相关测试 **已覆盖**（commit `63ed9cd37`） |

### A4. 注入与开关

| # | 用例 | 等级 | 落点 / 状态 |
|---|---|---|---|
| M-A4.1 | **H4**：`memory.enabled=false` 短路注入 setup，system prompt 不含 `# Long-term Memory`，日志不出现 backfill/consolidation/embedding 调用 | 🟢 自动 | `session-runtime-config.test.ts` **建议补** |
| M-A4.2 | 注入语义：feedback 全量 + 非 feedback 按 seedQuery 相关性 top-K | 🟢 自动 | `memory-reader.service.test.ts` **已覆盖** |
| M-A4.3 | seedQuery 不恒空（agent 描述 + workspace 名注入），相关性召回生效 | 🔵 集成 | **建议补** |

### A5. 整合 consolidation（M3）

| # | 用例 | 等级 | 落点 / 状态 |
|---|---|---|---|
| M-A5.1 | 触发门控：≥30 条 且 ≥7 天 且 空闲（fire-and-forget，注入时触发） | 🟢 自动 | `memory-consolidation.service.test.ts` **已覆盖** |
| M-A5.2 | MERGE：重复条目合并，keep 条目 description 更新，其余失效指向 keep | 🟢 自动 | `memory-consolidation.service.test.ts` **已覆盖** |
| M-A5.3 | ELEVATE 撞名保护：升华条目 name 不撞已有（commit `c38db285d`） | 🟢 自动 | `memory-consolidation.service.test.ts` **已覆盖** |
| M-A5.4 | 整合 job 触发 `log.info`（可追踪） | 🟢 自动 | **建议补**（spyOn logger） |

### A6. 错误隔离与清理

| # | 用例 | 等级 | 落点 / 状态 |
|---|---|---|---|
| M-A6.1 | **M11/M16**：delete/archive/inactive 时 `cleanupIndexOnInactive` 清 vec + entity_link（表不存在静默） | 🟢 自动 | `memory.repository.test.ts`（storage） **建议补** |
| M-A6.2 | **M12**：`bumpHit` 只更 `hit_count` + `last_hit_at`，不刷 `updated_at`（避免时间衰减马太效应） | 🟢 自动 | `memory.repository.test.ts`（storage） **建议补** |
| M-A6.3 | `listByScope` 安全 LIMIT 500（防极端库打满 IPC） | 🟢 自动 | **建议补** |

### A7. UI / IPC（MemoryPanel，commit `29d6dff17` / `ff09ed481` / `ed41b78d7`）

| # | 用例 | 等级 | 落点 / 状态 |
|---|---|---|---|
| M-A7.1 | settings:set `value=null` 触发 `repo.delete()`（表单清除语义修复） | 🟠 真机 | **建议补**：面板清空抽取 provider 下拉 → sqlite 确认 row 删除 |
| M-A7.2 | scope 切换清 scopeRef（workspaceId 不泄漏成 agentId） | 🟠 真机 | **建议补** |
| M-A7.3 | "测试抽取配置"按钮：主动验证连通性，成功/失败均有可读提示 | 🟠 真机 | **建议补** |
| M-A7.4 | 失效链视图：失效条目标灰 + ⚠️ 标注，可单独查看 | 🟠 真机 | **建议补** |
| M-A7.5 | DB 文件一致性：重建向量索引后 `memory_vec` 行数 = `memory_entry` 活跃行数（无假阳性） | 🟠 真机 | **建议补** |
| M-A7.6 | 记忆面板入口在「设置 → Agent 分组 → 记忆」二级菜单（commit `b7f3d784c`） | 🟠 真机 | **建议补**：核对导航路径 |

---

## 模块二：团队模式 A2A 深度协作 + 成员自由交流 v2

> 现状：`team-dispatch.service.test.ts` 30+ 用例、`team-roster-prompt.test.ts` 双视角、`session-runtime-config.test.ts` 团队段全覆盖。本节重点是**多 agent 真机协作剧本**与**codex 混编**。

### B1. 派发引擎（Phase B）

| # | 用例 | 等级 | 落点 / 状态 |
|---|---|---|---|
| T-B1.1 | `agent_message` 广播（无 target）：只写 `peer_message` 线程 + emit `team_peer_message`，**不触发**任何成员执行 | 🟢 自动 | `team-dispatch.service.test.ts` **已覆盖** |
| T-B1.2 | `agent_message` 定向 call（有 target + mode=call）：触发目标一次完整 turn，写 `peer_message` + `member_reply` 两条线程 | 🟢 自动 | **已覆盖** |
| T-B1.3 | 定向 note（mode=note）：只写线程 + emit `delivery=note`，不触发执行 | 🟢 自动 | **已覆盖**（commit `304307ec5`） |
| T-B1.4 | 越权 target 拒绝（target 不在启用成员） | 🟢 自动 | **已覆盖** |
| T-B1.5 | self-@ 拒绝（避免成员给自己触发新一轮执行） | 🟢 自动 | **已覆盖** |
| T-B1.6 | 讨论消息总量超限（40/讨论）跨 service 实例持续生效（持久化线程计数） | 🟢 自动 | **已覆盖** |
| T-B1.7 | 无 discussionRepo 时返回 `no_discussion_repo`（workflow 编排路径） | 🟢 自动 | **已覆盖** |
| T-B1.8 | 与既有 dispatch 路径互不干扰：run 无 discussionId 时不写线程 | 🟢 自动 | **已覆盖** |

### B2. 防失控预算（Phase 0b + 成员自由交流 P3）

| # | 用例 | 等级 | 落点 / 状态 |
|---|---|---|---|
| T-B2.1 | 正文 @ 自动链：链深上限 `MAX_AUTO_MENTION_HOPS=6`，第 7 跳被拦 | 🟢 自动 | **已覆盖** |
| T-B2.2 | 同对同轮双向定向消息上限 8 条（`MAX_DIRECTED_EXCHANGES_PER_PAIR_PER_ROUND`） | 🟢 自动 | **已覆盖** |
| T-B2.3 | note 不计入 pair 往返上限（8 条 call 上限外再发 note 仍成功），但计入消息总量 | 🟢 自动 | **已覆盖** |
| T-B2.4 | 同步咨询深度上限 3：第 4 层 call 被拒 `consult_depth_exceeded`，文案引导改用 note | 🟢 自动 | **已覆盖** |
| T-B2.5 | deadline 剩余 <30s 时 directed call 被拒，文案含 "note" 引导 | 🟢 自动 | **已覆盖** |
| T-B2.6 | peer call 独立预算（20/turn）与 host dispatch 预算（10/turn）互不挤占 | 🟢 自动 | **已覆盖** |
| T-B2.7 | 成员发起的定向 @ 不受嵌套深度限制（currentDepth>0 + allowNesting=false 仍成功） | 🟢 自动 | **已覆盖** |
| T-B2.8 | 定向 @ 绕过 turn 串行队列：发起者占用队列槽位执行中也不死锁 | 🟢 自动 | **已覆盖** |

### B3. 花名册 Prompt 双视角（Phase C）

| # | 用例 | 等级 | 落点 / 状态 |
|---|---|---|---|
| T-B3.1 | Host 视角：含 `team_round_advance` / `team_conclude`（替代旧 CONVERGE 劝诫） | 🟢 自动 | `team-roster-prompt.test.ts` **已覆盖** |
| T-B3.2 | Member 视角：含 role + peer messaging + thread snippet | 🟢 自动 | **已覆盖** |
| T-B3.3 | **P1 四模式手册**：成员 prompt 含 MODE 2 "in this very turn" + MODE 4 "note" 关键词 | 🟢 自动 | **已覆盖**（commit `304307ec5`） |
| T-B3.4 | enablePeerMessaging=false 时省略 agent_message 引导 | 🟢 自动 | **已覆盖** |
| T-B3.5 | Host 视角：peer messaging 开启时告诉 host 不要替成员转发咨询 | 🟢 自动 | **已覆盖** |

### B4. 会话连续性 + 工具挂载（Phase D / runtime config）

| # | 用例 | 等级 | 落点 / 状态 |
|---|---|---|---|
| T-B4.1 | enablePeerMessaging=true + nesting=false：成员只拿到 `agent_message`（无 agent_dispatch） | 🟢 自动 | `session-runtime-config.test.ts` **已覆盖** |
| T-B4.2 | @-mentioned 成员：拿到 roster + agent_message 工具，**不剥离**其工作工具 | 🟢 自动 | **已覆盖** |
| T-B4.3 | dispatched member 尊重 resume-safety gate（`sdkResumeSafe` + previousPromptSnapshot） | 🟢 自动 | **已覆盖** |
| T-B4.4 | codex host + 团队成员：挂载 spark_team HTTP 桥（FR-0b） | 🟢 自动 | **已覆盖** |

### B5. UI 呈现（commit `304307ec5` 前端）

| # | 用例 | 等级 | 落点 / 状态 |
|---|---|---|---|
| T-B5.1 | `team_peer_message` 气泡显示 sender → target，note 加"留言"角标 | 🟠 真机 | `team-member-bubble.test.tsx` **已覆盖** |
| T-B5.2 | 嵌套咨询应答：`team_member_message` 带 `inReplyToAgentId`，气泡显示「应 X 的咨询」 | 🟠 真机 | **建议补** |
| T-B5.3 | 讨论侧栏按 `[R{n}]` 轮次分组渲染线程 | 🟠 真机 | **建议补** |
| T-B5.4 | ChatView 团队消息可见性（`chat-team-visibility.ts`） | 🟢 自动 | `chat-team-visibility.test.ts` **已覆盖** |
| T-B5.5 | team 事件映射（`event-mapper.ts`）兼容老事件无 `delivery` 字段 | 🟢 自动 | `team-events.test.ts` **已覆盖** |

### B6. 真机协作剧本（C 级，最重要）

| # | 场景 | 期望 | 等级 |
|---|---|---|---|
| T-B6.1 | @B 问一个**必须 C 参与的问题** | B 的工具调用记录里有 turn 内 `agent_message → C`，最终回复融合 C 信息一次性返回（**不**告诉 asker "我去问下 C"然后结束 turn） | 🟠 |
| T-B6.2 | B 给 C 留 note 后结束 | C 不被触发；下次派发 C 时其 prompt 含 `[NOTE FOR YOU]`，回复体现按留言行动 | 🟠 |
| T-B6.3 | "你俩互相对话三轮" | 正文 @ 自动链完成 ≥3 个往返，不经 Host 转发，8 条 pair 上限内不被误拦 | 🟠 |
| T-B6.4 | 构造 A→B→C→D 四层咨询 | 第 4 层被 `consult_depth_exceeded` 拦，B/C 收到可读引导 | 🟠 |
| T-B6.5 | **claude Host + codex 成员混编**重跑 B6.1 | codex 成员经 HTTP 桥接同样完成 turn 内咨询（Phase 0 codex 分流验证） | 🟠 |
| T-B6.6 | Host 调 `team_round_advance` 推进轮次 | UI 出现轮次分割线，`roundIndex + 1`，可继续派发 | 🟠 |
| T-B6.7 | Host 调 `team_conclude` | 讨论标记结束，后续 dispatch 被拒（除非用户发新 turn） | 🟠 |
| T-B6.8 | `maxDiscussionRounds` 硬上限（默认 6，最大 20）兜底拒绝 | 第 N+1 轮 `team_round_advance` 被拒 | 🟠 |

---

## 模块三：工作流升级

> 现状：`workflow-executor.test.ts` 28 例、`workflow-atomic-execution.test.ts` 42 例、`workflow-templates.test.ts` 63 例，共 133/133 全绿。本节重点是**端到端真实执行**与**条件分支**。

### C1. 原子节点真实执行（commit `86668ef99`）

| # | 用例 | 等级 | 落点 / 状态 |
|---|---|---|---|
| W-C1.1 | `shouldRunWorkflowAtomicNodeAsAgent`：skill/tool/mcp/plan/review/artifact 默认真实执行；verify/approval/agent/subagent 走专用路径 | 🟢 自动 | `workflow-atomic-execution.test.ts` **已覆盖** |
| W-C1.2 | `config.execution='static'` 强制回落静态回显（含 input） | 🟢 自动 | **已覆盖** |
| W-C1.3 | input 节点 LLM 结构化解析（目标/约束/交付物三段），合法 JSON 原样保留（含 ```json fence） | 🟢 自动 | **已覆盖** |
| W-C1.4 | input 非法 JSON → 回落透传 fallback + 追加 `[input 结构化解析失败]` 提示 | 🟢 自动 | **已覆盖** |
| W-C1.5 | input execution=auto 但 worker 未注册 → 回落透传 | 🟢 自动 | **已覆盖** |
| W-C1.6 | skill 节点只挂所选 Skill；tool 节点收窄 toolIds 白名单；mcp 节点只挂所选服务 | 🟢 自动 | **已覆盖**（disallowedTools = 全量 - 白名单） |
| W-C1.7 | plan/review 强制只读工具集（禁写/禁 Bash） | 🟢 自动 | **建议补**：断言 disallowedTools 含 Write/Edit/Bash |
| W-C1.8 | artifact `exportPath` 写盘 + 路径穿越防护（`../` 拒、绝对路径拒、兄弟目录 root-evil 不误判） | 🟢 自动 | **已覆盖**（`resolveWorkflowArtifactExportPath`） |
| W-C1.9 | atomic workerId `workflow-atomic:${nodeId}` 与 agent/subagent workerId 命名空间隔离 | 🟢 自动 | **已覆盖** |

### C2. 条件分支编排

| # | 用例 | 等级 | 落点 / 状态 |
|---|---|---|---|
| W-C2.1 | 条件边双向序列化（此前保存即丢失） | 🟢 自动 | `graph-adapter` **建议补**：构造带 true/false 双条件的图，序列化→反序列化 round-trip |
| W-C2.2 | 边检查器 5 种算子 + 状态键 + 比较值，true/false/null/数字自动还原类型 | 🟢 自动 | **建议补** |
| W-C2.3 | 跳过 inactive 条件边下游节点 + 其后代 | 🟢 自动 | `workflow-executor.test.ts` **已覆盖** |
| W-C2.4 | 执行 active 条件边下游 + 传 active upstream inputs | 🟢 自动 | **已覆盖** |
| W-C2.5 | 条件边虚线 + 警示色 + label（如 `plan_ok = true`） | 🟠 真机 | **建议补**：截图核对样式 |

### C3. 并行与子代理

| # | 用例 | 等级 | 落点 / 状态 |
|---|---|---|---|
| W-C3.1 | subagent `parallelism=3` fan-out：N 路并发 dispatch，结果按 `--- branch i ---` 拼接 | 🟢 自动 | `workflow-executor.test.ts` **已覆盖** |
| W-C3.2 | 一条 fan-out 分支失败 → 节点失败，记录所有分支 | 🟢 自动 | **已覆盖** |
| W-C3.3 | parallelism 未设或 ≤1 → 仅 dispatch 一次 | 🟢 自动 | **已覆盖** |
| W-C3.4 | 独立 ready 节点同 wave 并发，downstream join | 🟢 自动 | **已覆盖** |
| W-C3.5 | agent 节点 `agentId` 为空 → `missing_agent_id` 失败（不再静默剔除） | 🟢 自动 | **已覆盖** |
| W-C3.6 | agent 节点 unbound/不可用 → 回落 host agent | 🟢 自动 | **已覆盖** |
| W-C3.7 | 一个 agent 空 agentId 与健康节点并存 → 整个 workflow 终止 | 🟢 自动 | **已覆盖** |

### C4. 审批门禁

| # | 用例 | 等级 | 落点 / 状态 |
|---|---|---|---|
| W-C4.1 | approval approve/reject/cancelled/declined/skipped → 批准/未批准判定 | 🟢 自动 | **已覆盖** |
| W-C4.2 | approval 双问询（决策 + 修改意见），comment 经 `state[outputKey]` 流向下游 | 🟢 自动 | **已覆盖** |
| W-C4.3 | approve + comment → content 含 `[审批修改意见]` 段 | 🟢 自动 | **已覆盖** |
| W-C4.4 | approval 不重试（即便配 retryCount） | 🟢 自动 | **已覆盖** |
| W-C4.5 | verify 节点失败重试到 retryCount 后放弃 | 🟢 自动 | **已覆盖** |

### C5. 模板库（commit `3b584432a`）

| # | 用例 | 等级 | 落点 / 状态 |
|---|---|---|---|
| W-C5.1 | 模板 id 全局唯一；至少 8 个模板；11 类节点全覆盖 | 🟢 自动 | `workflow-templates.test.ts` **已覆盖** |
| W-C5.2 | 每模板：节点 id 唯一、kind 合法、坐标有限数字、边 id 唯一无自环 | 🟢 自动 | **已覆盖** |
| W-C5.3 | 条件边 key 引用图中某节点 outputKey | 🟢 自动 | **已覆盖** |
| W-C5.4 | 每模板是 DAG（无回边，可拓扑排序） | 🟢 自动 | **已覆盖** |
| W-C5.5 | 每模板含 input 起点节点 | 🟢 自动 | **已覆盖** |
| W-C5.6 | 「模板库」按钮 / 空状态「从模板开始」入口；导入即落库为 draft | 🟠 真机 | **建议补** |
| W-C5.7 | 绑定类字段（agentId/skillIds/toolIds/mcpServerIds）导入后留空，由用户补齐 | 🟠 真机 | **建议补** |

### C6. 端到端真机剧本（C 级）

| # | 场景 | 期望 | 等级 |
|---|---|---|---|
| W-C6.1 | 标准研发流（input→plan→agent→verify→artifact） | 各节点真实派发，artifact 写盘到 exportPath | 🟠 |
| W-C6.2 | 审批门禁流：approval reject | workflow 在 approval 处停止，下游不执行 | 🟠 |
| W-C6.3 | 并行草案评审（subagent parallelism=3） | 3 路并发，结果拼接，UI 可见 3 个 branch | 🟠 |
| W-C6.4 | 只读调研报告（review 链 + artifact exportPath） | review 节点禁写禁 Bash，artifact 写盘 | 🟠 |
| W-C6.5 | 条件路由流（plan equals 决策，二分支独立终点） | 走对应分支，另一分支节点不执行 | 🟠 |
| W-C6.6 | workflow_run 实时进度（`WorkflowProgressEvent`） | UI 任务面板按节点渲染进度快照 | 🟠 |
| W-C6.7 | 宿主进入编排模式（`OrchestrationStatusEvent`） | UI 与 agent 自身被告知，不再撞 "tool not enabled" | 🟠 |

---

## 模块四：交叉回归（三大模块联动）

> 这几天三个模块都在动 `session.service.ts` 与派发引擎，必须验证互不破坏。

| # | 用例 | 等级 | 状态 |
|---|---|---|---|
| X-1 | 团队模式下记忆注入仍生效：团队会话 turn 结束后，成员对话也触发记忆抽取/注入 | 🔵 集成 | **建议补** |
| X-2 | 工作流 atomic 节点派发的临时 worker 与团队 `memberAgentIds` 命名空间隔离，互不污染 | 🟢 自动 | **已覆盖**（`workflowAtomicMemberId` + `allowedWorkerIds` 测试） |
| X-3 | 工作流编排态 host 工具收窄（hasWorkflowExecutableNodes）不影响记忆 `search_memory`/`recall_memory` 工具可用 | 🟠 真机 | **建议补** |
| X-4 | codex host 同时启用团队 + 记忆：spark_team HTTP 桥 + spark-memory-mcp-server.mjs stdio 桥共存不冲突 | 🟠 真机 | **建议补** |
| X-5 | `memory.enabled=false` + 团队模式：记忆短路不影响团队派发 | 🟢 自动 | **建议补** |
| X-6 | 工作流节点 dispatch 失败时，记忆 `maybeWriteFromTurn` 仍能从已产生的事件抽取（fire-and-forget 不阻塞） | 🔵 集成 | **建议补** |

---

## 执行清单（给 QA / 开发者）

### 自动化（🟢 A 级，进 CI）

```bash
# 一键全跑
pnpm --filter @spark/agent-runtime test
pnpm --filter @spark/storage test          # 需切 Node ABI（见 commit 10a8d8680 说明）
pnpm --filter @spark/protocol typecheck
pnpm -C apps/desktop test                   # 前端 vitest
```

**建议新增的 A 级用例落点**（共 ~20 条，已在表中标注「建议补」）：
- `packages/agent-runtime/src/services/memory/memory-extraction.prompt.test.ts`（新建）
- `packages/agent-runtime/src/services/memory/memory-writer.service.test.ts`（补 H7/M14）
- `packages/agent-runtime/src/services/memory/memory-search.service.test.ts`（补 H2 负缓存）
- `packages/agent-runtime/src/services/memory/memory-reader.service.test.ts`（补失效 recall）
- `packages/storage` 下 memory.repository 测试（补 H3/M11/M12/M9）
- `packages/agent-runtime/src/services/workflow-graph-adapter.test.ts`（新建，条件边 round-trip）
- `packages/agent-runtime/src/services/workflow-atomic-execution.test.ts`（补 plan/review 只读断言）

### 集成（🔵 B 级，本机真实 DB + 真实 provider）

```bash
# 1. 切 better-sqlite3 到 Node ABI（Electron abi=125 会 NODE_MODULE_VERSION 不匹配）
# 2. 跑记忆 e2e
node packages/agent-runtime/scripts/smoke-test-memory-extraction.mjs
pnpm --filter @spark/agent-runtime test memory-e2e
```

### 真机（🟠 C 级，desktop 手测）

```bash
pnpm dev
# 按 todo/记忆系统-v2-端到端测试用例.md 7 场景 + 本文档 T-B6.* / W-C6.* 逐条跑
```

---

## 附：本轮改动 commit 清单（核对用）

**记忆系统 V2**（M1→M4 + 审查修复）：
`8187cd445` `f0e2b1b11` `9206787cf` `818769a23` `f45f2c8ae` `93dcf96d0` `6181b5599` `63ed9cd37` `51dcc6532` `22b9d9e26` `477f1e45a` `10a8d8680` `c38db285d` `ed41b78d7` `ff09ed481` `73cdfaa91` `89f61db84` `e955e81e7` `8f3f83552` `ff70deabb`

**团队模式 A2A**（Phase 0→G）：
`0d2712283` `433d68a86` `268b71bdb` `8787adba3` `ac5905ef1` `9ada968e5` `304307ec5` `47be342fa`

**工作流升级**：
`f0e65aca0` `86668ef99` `3b584432a` `e45545d94`

---

> **核对约定**：跑完后请把每条「状态」改为 `✅ 通过` / `❌ 失败（附现象）` / `⏭ 跳过（附原因）`，并在文档头刷新「最后核对」日期。失败项请回贴日志片段，便于定位是回归还是预存在问题。
