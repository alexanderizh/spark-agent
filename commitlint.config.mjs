/**
 * 提交规范固化（工程化还债方案 Phase 0.4）。
 * Conventional Commits 达 95% 的存量证明团队习惯与之兼容，这里只做机器固化。
 * 中文 subject 合法：config-conventional 的 subject-case 规则不约束中文字符。
 */
export default {
  extends: ['@commitlint/config-conventional'],
}
