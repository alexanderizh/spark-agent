# ===============================================================
# Spark Agent 官网 环境变量
# 拷贝为 .env.local 后填写本地开发用的值；
# 生产构建时通过 Dockerfile 的 --build-arg 注入到镜像里（见 website-cicd skill）。
# ===============================================================

# 自建版本中心 API 基地址（edu-server）。
# - 留空时浏览器自动回退到 window.location.origin，适合官网与 edu-server
#   通过同一个 nginx 反代的部署（推荐：/api/v1/* → edu-server:7002）。
# - 跨域部署时填完整公网 URL，例如：
#     https://spark.yiqibyte.com
# 构建脚本（scripts/fetch-downloads.mjs）会同时读这个值，将首屏数据烘进 dist/。
VITE_RELEASES_API_BASE=https://spark.example.com
